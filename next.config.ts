/**
 * @file next.config.ts
 * @version 0.1.0
 * @description Konfigurasi build Next.js Parissa POS.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ["127.0.0.1"],
};

export default nextConfig;
